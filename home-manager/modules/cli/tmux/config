bind r source-file ~/.config/tmux/tmux.conf \; display-message "RELOADING CONFIGURATION FILE…"

unbind C-b
set-option -g prefix C-w
bind-key C-w send-prefix

set -s escape-time 50 

# Styling
set-option -g status-position top
set-option -g status-justify centre

set -g status-right '#S @ #h'
set -g status-left '#I #W'
set-option -g status-left-length 30
set-window-option -g window-status-current-format 'C %b %d, %Y  T %H:%M '
# Disable showing the default window list component.
set-window-option -g window-status-format ''

